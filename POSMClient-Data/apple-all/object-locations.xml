<object-locations>
  <pages>
  </pages>

  <images>
  </images>

  <css>
  </css>

  <js>
    <file folder="apple-all" name="posMClient/dojoCustomSrc/posmclient/HeaderToolBar" location="apple-all" />
  </js>

  <menus>
    <file folder="apple-all" name="posMClient/pos_manager_key.ovcmnu" location="apple-all" />
    <file folder="apple-all" name="posMClient/pos_checkout_receipts.ovcmnu" location="apple-all" />
    <file folder="apple-all" name="posMClient/pos_checkout.ovcmnu" location="apple-all" />
  </menus>

  <processes>
  </processes>

  <actions>
  </actions>

  <templates>
  </templates>

  <resources>
  </resources>

  <config>
    <file folder="apple-all" name="posMClient/pos.ovccfg" location="apple-all" />
    <file folder="apple-all" name="posMClient/alerts.ovccfg" location="apple-all" />
    <file folder="apple-all" name="posMClient/cometdGatewayMapping.ovccfg" location="apple-all" />
  </config>

  <sql>
  </sql>

  <gateways>
  </gateways>

  <listeners>
  </listeners>

   <htmlTemplates>
  </htmlTemplates>
</object-locations>